public interface bycycle { 
	void applyBrake();// Interfaces are the class in which all the interface methods are the included without the body , if any class implements the interface they have to use all the interface methods.
}